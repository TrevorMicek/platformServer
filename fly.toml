# fly.toml app configuration file generated for edit-api on 2023-04-26T19:43:05-06:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = "edit-api"
primary_region = "den"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true

[checks]
  [checks.alive]
    type = "tcp"
    interval = "15s"
    timeout = "2s"
    grace_period = "5s"
